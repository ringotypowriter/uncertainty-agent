{
  "name": "uncertainty-agent",
  "version": "0.1.0",
  "description": "Measurement uncertainty pipeline with staged SubAgents",
  "license": "MIT",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "prestart": "tsc",
    "start": "bun run dist/main.js",
    "contract-md": "bun run scripts/generate-contract-md.ts",
    "check": "tsc --noEmit",
    "testset": "bun run src/testset-pipeline.ts"
  },
  "dependencies": {
    "@mariozechner/pi-agent-core": "^0.70.0",
    "@mariozechner/pi-ai": "^0.70.0",
    "@sinclair/typebox": "^0.34.49",
    "chalk": "^4.1.2"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=20"
  }
}
